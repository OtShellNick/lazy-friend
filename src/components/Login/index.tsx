import { LoginCard } from '@blocks/LoginCard';

export const Login = () => {
  return (
    <div className='flex items-center h-full justify-center bg-gradient-to-b from-stone-100 to-stone-300'>
      <LoginCard />
    </div>
  );
};
