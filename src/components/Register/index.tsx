import { RegisterCard } from '@blocks/RegisterCard';

export const Register = () => {
  return (
    <main className='flex items-center h-[100vh] justify-center bg-gradient-to-b from-stone-100 to-stone-300'>
      <RegisterCard />
    </main>
  );
};
